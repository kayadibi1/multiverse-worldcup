// Shared scene constants in their own module to avoid Globe<->Traces/Flags import cycles.
export const GLOBE_R = 2.5
// The 2026 final venue (MetLife Stadium, East Rutherford NJ) — the convergence + dive target.
export const HOST_LATLON: [number, number] = [40.81, -74.07]
